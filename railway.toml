[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/python run_dashboard.py --no-browser"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"

[[services]]
name = "aria-os-dashboard"
